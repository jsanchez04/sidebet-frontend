# Base URL of the Sidebet backend API.
# Copy this file to .env.local and adjust as needed for local development.
# In production (e.g. Vercel), set VITE_API_URL to your deployed backend URL.
VITE_API_URL=http://localhost:8080
